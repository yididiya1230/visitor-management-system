{
  "$schema": "https://schema.railpack.com",
  "provider": "dotnet",
  "packages": {
    "dotnet": "10.0"
  },
  "steps": {
    "install": {
      "commands": [
        "cd src/ECX.VisitorManagement.API && dotnet restore ECX.VisitorManagement.API.csproj"
      ]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": [
        "cd src/ECX.VisitorManagement.API && dotnet publish ECX.VisitorManagement.API.csproj --no-restore -c Release -o /app/out"
      ]
    }
  },
  "deploy": {
    "startCommand": "./out/ECX.VisitorManagement.API"
  }
}
